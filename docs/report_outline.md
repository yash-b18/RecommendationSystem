# Report Outline: Explainable Multi-Stage E-Commerce Recommendation System

> Fill in each section using outputs generated by the pipeline.
> Artifacts are in `data/outputs/` and `data/outputs/figures/`.

---

## 1. Problem Statement
- Describe the recommendation problem (top-K item ranking for e-commerce users).
- Motivate why explainability matters in this setting.
- Define the prediction task formally: given user interaction history, predict a ranked list of products the user is likely to engage with positively.

## 2. Data Sources
- Amazon Reviews 2023 dataset (McAuley-Lab/Amazon-Reviews-2023 on HuggingFace).
- Category: Video Games.
- Describe statistics: number of users, items, interactions, sparsity.
- Describe metadata fields used: title, category, brand, price, description.
- Include EDA plots from `data/outputs/figures/eda_*.png`.

## 3. Related Work
- Matrix Factorization (Koren et al., 2009).
- Neural Collaborative Filtering (He et al., 2017).
- Two-Tower architectures (Yi et al., 2019 — YouTube DNN).
- LightGBM for ranking (Microsoft LightGBM paper).
- SHAP for model explainability (Lundberg & Lee, 2017).
- Amazon Reviews dataset papers (McAuley et al.).
- State what prior approaches on this dataset exist and what this project adds:
  - Multi-stage pipeline
  - Explainability at inference
  - Rigorous ablation experiment

## 4. Evaluation Strategy & Metrics
- Justify choice of ranking metrics over accuracy metrics.
- Metrics used: Recall@K, Hit Rate@K, NDCG@K, MRR.
- Explain K values evaluated: 5, 10, 20.
- Describe leave-one-out or random split evaluation protocol.
- Reference `data/outputs/metrics.csv` for quantitative results.

## 5. Modeling Approach

### 5.1 Data Processing Pipeline
- Filtering: min user/item reviews.
- Positive/negative sampling strategy.
- Train/val/test split (temporal or random).
- Label encoding of user/item IDs.
- Feature normalization.

### 5.2 Hyperparameter Tuning Strategy
- LightGBM: early stopping on validation NDCG@10.
- Two-Tower: patience-based early stopping on val loss.
- Describe any grid search or manual tuning performed.

### 5.3 Models Evaluated

#### Naive Baseline
- Global popularity recommender.
- Top-rated-by-category recommender.
- Rationale: provides lower bound; popular items are a strong non-trivial baseline.

#### Classical Model (LightGBM)
- Describe feature set (see Section 5.1 pipeline).
- Rationale: strong gradient boosted model; interpretable via SHAP; fast to train.
- Reference feature importance plot: `data/outputs/figures/feature_importance.png`.

#### Deep Learning Model (Two-Tower)
- Architecture: separate user tower and item tower with shared embedding space.
- Inputs: user ID embedding + item ID embedding + metadata features.
- Rationale: captures non-linear interaction patterns; scales to large catalogs.

## 6. Results
- Comparison table across all models and metrics (reference `data/outputs/metrics_table.md`).
- Key takeaways (which model wins, by how much, at what cost).
- Include figures from `data/outputs/figures/`.

## 7. Error Analysis
- Describe methodology (see `scripts/error_analysis.py`).
- Present 5 specific mispredictions with:
  - User history summary
  - Predicted top-K
  - Actual relevant item
  - Model score
  - Root cause
  - Proposed mitigation
- Reference `data/outputs/error_analysis.csv`.

## 8. Experiment Write-Up (Feature Ablation)

### Experimental Plan
- Motivation: understand contribution of each feature group.
- Ablation sets: ID-only → +metadata → +text → +user-history aggregation.
- Held constant: model architecture, training procedure, evaluation protocol.

### Results
- Reference `data/outputs/experiment/ablation_results.csv`.
- Include `data/outputs/experiment/ablation_ndcg.png`.

### Interpretation
- Which feature group contributes the most?
- Diminishing returns from text features?
- Does user-history aggregation help?

### Recommendations
- Which feature set to use for production deployment.

## 9. Recommendations
- Based on results, recommend the best model for deployment.
- Trade-offs: latency, explainability, performance.

## 10. Conclusions
- Summary of findings.
- Does the multi-stage pipeline outperform baselines?
- Is explainability achievable without sacrificing much accuracy?

## 11. Future Work
- With another semester: incorporate session-based recommendation, larger dataset, LLM-based explanations.
- Real-time user feedback loop.
- A/B testing framework.

## 12. Commercial Viability Statement
- Discuss applicability to a real e-commerce platform.
- Latency considerations for real-time serving.
- Cold-start handling.
- Data privacy and GDPR considerations.

## 13. Ethics Statement
- Recommendation systems can amplify popularity bias.
- Filter bubble and diversity of recommendations.
- User data privacy.
- Potential for manipulative recommendations (sponsored items).
- Mitigation strategies.
